{
  "name": "sanity-nuxt-starter",
  "private": true,
  "scripts": {
    "dev": "npm run dev:studio & npm run dev:nuxt",
    "dev:studio": "npm run dev --prefix studio",
    "dev:nuxt": "npm run dev --prefix nuxt",
    "build": "npm run build --prefix studio && npm run build --prefix nuxt",
    "deploy:studio": "npm run deploy --prefix studio",
    "migrate": "npm run migrate --prefix studio"
  }
}
